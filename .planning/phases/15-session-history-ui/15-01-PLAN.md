---
phase: 15-session-history-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/history.ts
  - src/popup/history_helpers.ts
  - src/background/serviceWorker.ts
  - tests/test_history.ts
  - tests/test_history_helpers.ts
autonomous: true
requirements: [HIST-04, HIST-08, HIST-09]

must_haves:
  truths:
    - "deleteSessionFromHistory removes a single session by report_id from storage"
    - "clearAllHistory sets SESSION_HISTORY to an empty array"
    - "EXPORT_CSV_FROM_DATA message type generates CSV from inline data and triggers download"
    - "formatRelativeDate returns 'Today', 'Yesterday', or short date string"
    - "formatClubSummary truncates club list to 3 with '+N more' suffix"
  artifacts:
    - path: "src/shared/history.ts"
      provides: "deleteSessionFromHistory and clearAllHistory exports"
      exports: ["deleteSessionFromHistory", "clearAllHistory"]
    - path: "src/popup/history_helpers.ts"
      provides: "formatRelativeDate, formatClubSummary, countSnapshotShots pure functions"
      exports: ["formatRelativeDate", "formatClubSummary", "countSnapshotShots"]
    - path: "src/background/serviceWorker.ts"
      provides: "EXPORT_CSV_FROM_DATA message handler"
      contains: "EXPORT_CSV_FROM_DATA"
    - path: "tests/test_history.ts"
      provides: "Tests for delete and clear functions"
    - path: "tests/test_history_helpers.ts"
      provides: "Tests for date formatting and club summary"
  key_links:
    - from: "src/shared/history.ts"
      to: "chrome.storage.local"
      via: "get/set with SESSION_HISTORY key"
      pattern: "STORAGE_KEYS\\.SESSION_HISTORY"
    - from: "src/background/serviceWorker.ts"
      to: "src/shared/csv_writer.ts"
      via: "writeCsv call with inline data"
      pattern: "EXPORT_CSV_FROM_DATA"
---

<objective>
Add history module functions (delete, clear), pure UI helper functions, and a new CSV export message type for historical sessions.

Purpose: Provide the backend contracts that Plan 02 will wire into the popup UI. The delete/clear functions, date/club formatting helpers, and inline CSV export path must all exist before the popup can use them.
Output: Updated history.ts with delete/clear exports, new history_helpers.ts with pure functions, updated serviceWorker.ts with EXPORT_CSV_FROM_DATA handler, and tests for all new code.
</objective>

<execution_context>
@/Users/kylelunter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kylelunter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-session-history-ui/15-CONTEXT.md
@.planning/phases/15-session-history-ui/15-RESEARCH.md

@src/shared/history.ts
@src/shared/constants.ts
@src/models/types.ts
@src/background/serviceWorker.ts
@tests/test_history.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/models/types.ts:
```typescript
export interface SessionData {
  date: string;
  report_id: string;
  url_type: "report" | "activity";
  club_groups: ClubGroup[];
  raw_api_data?: unknown;
  metric_names: string[];
  metadata_params: Record<string, string>;
}

export type SessionSnapshot = Omit<SessionData, "raw_api_data">;

export interface HistoryEntry {
  captured_at: number;
  snapshot: SessionSnapshot;
}

export interface ClubGroup {
  club_name: string;
  shots: Shot[];
  averages: Record<string, MetricValue>;
  consistency: Record<string, MetricValue>;
}
```

From src/shared/constants.ts:
```typescript
STORAGE_KEYS.SESSION_HISTORY // = "sessionHistory"
```

From src/shared/history.ts (existing exports):
```typescript
export function saveSessionToHistory(session: SessionData): Promise<void>;
export function getHistoryErrorMessage(error: string): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add delete/clear functions to history.ts and pure helpers in history_helpers.ts</name>
  <files>src/shared/history.ts, src/popup/history_helpers.ts, tests/test_history.ts, tests/test_history_helpers.ts</files>
  <behavior>
    - deleteSessionFromHistory("report-1") removes the entry with report_id "report-1" from SESSION_HISTORY storage
    - deleteSessionFromHistory("nonexistent") resolves without error when report_id not found
    - deleteSessionFromHistory rejects on chrome.runtime.lastError
    - clearAllHistory sets SESSION_HISTORY to empty array
    - clearAllHistory rejects on chrome.runtime.lastError
    - formatRelativeDate returns "Today" for timestamps from today
    - formatRelativeDate returns "Yesterday" for timestamps from yesterday
    - formatRelativeDate returns "Mar 5" format for older dates
    - formatClubSummary(["7i"]) returns "7i"
    - formatClubSummary(["7i", "PW", "9i"]) returns "7i, PW, 9i"
    - formatClubSummary(["7i", "PW", "9i", "D", "3W"]) returns "7i, PW, 9i +2 more"
    - countSnapshotShots counts total shots across all club_groups
    - countSnapshotShots returns 0 for empty club_groups
  </behavior>
  <action>
    1. Add tests to `tests/test_history.ts` for `deleteSessionFromHistory` and `clearAllHistory`:
       - Follow the existing test pattern with `mockStore` and `beforeEach` reset
       - Test delete by report_id (removes correct entry, leaves others)
       - Test delete of nonexistent report_id (no-op)
       - Test delete rejects on `chrome.runtime.lastError`
       - Test clearAllHistory sets storage to empty array
       - Test clearAllHistory rejects on error

    2. Add `deleteSessionFromHistory(reportId: string): Promise<void>` to `src/shared/history.ts`:
       - Read SESSION_HISTORY, filter out entry matching report_id, write back
       - Follow exact same Promise-wrapping pattern as `saveSessionToHistory`

    3. Add `clearAllHistory(): Promise<void>` to `src/shared/history.ts`:
       - Set SESSION_HISTORY to empty array
       - Follow same error handling pattern

    4. Create `src/popup/history_helpers.ts` with three pure exported functions:
       - `formatRelativeDate(timestamp: number): string` — "Today" if same calendar day, "Yesterday" if previous day, else `toLocaleDateString("en-US", { month: "short", day: "numeric" })`
       - `formatClubSummary(clubGroups: ClubGroup[]): string` — map to club_name, join first 3 with ", ", append ` +N more` if length > 3
       - `countSnapshotShots(snapshot: SessionSnapshot): number` — sum of shots.length across all club_groups

    5. Create `tests/test_history_helpers.ts` with vitest tests for all three functions. No chrome mock needed — these are pure functions. Import ClubGroup and SessionSnapshot types.

    Run RED tests first, then implement, then GREEN.
  </action>
  <verify>
    <automated>cd /Users/kylelunter/claudeprojects/trackv3 && npx vitest run tests/test_history.ts tests/test_history_helpers.ts</automated>
  </verify>
  <done>deleteSessionFromHistory, clearAllHistory, formatRelativeDate, formatClubSummary, countSnapshotShots all exported and passing tests</done>
</task>

<task type="auto">
  <name>Task 2: Add EXPORT_CSV_FROM_DATA message type to service worker</name>
  <files>src/background/serviceWorker.ts</files>
  <action>
    Add a new message handler for `EXPORT_CSV_FROM_DATA` in `serviceWorker.ts`. This is needed because the existing `EXPORT_CSV_REQUEST` reads data from `TRACKMAN_DATA` storage (which is the live session), but historical sessions only exist in `cachedData` in the popup.

    1. Add interface:
       ```typescript
       interface ExportCsvFromDataRequest {
         type: "EXPORT_CSV_FROM_DATA";
         data: SessionData;
       }
       ```

    2. Add to the `RequestMessage` union type.

    3. Add handler block (before the closing of onMessage.addListener):
       - Extract `data` from message (the inline SessionData)
       - Read unit prefs and surface from storage (same as existing EXPORT_CSV_REQUEST)
       - Call `writeCsv(data, includeAverages, undefined, unitChoice, surface)`
       - Trigger `chrome.downloads.download` with the CSV
       - Return success/error via `sendResponse`
       - The handler should mirror the existing EXPORT_CSV_REQUEST handler's unit preference resolution and download logic exactly, but use `message.data` instead of reading `TRACKMAN_DATA` from storage.

    Do NOT modify the existing `EXPORT_CSV_REQUEST` handler — it continues to work for live sessions.
  </action>
  <verify>
    <automated>cd /Users/kylelunter/claudeprojects/trackv3 && bash scripts/build-extension.sh</automated>
  </verify>
  <done>EXPORT_CSV_FROM_DATA handler exists in serviceWorker.ts, build succeeds with no TypeScript errors</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/test_history.ts tests/test_history_helpers.ts` — all tests pass
- `bash scripts/build-extension.sh` — clean build with no errors
- `src/shared/history.ts` exports deleteSessionFromHistory and clearAllHistory
- `src/popup/history_helpers.ts` exports formatRelativeDate, formatClubSummary, countSnapshotShots
- `src/background/serviceWorker.ts` handles EXPORT_CSV_FROM_DATA message type
</verification>

<success_criteria>
- All 5 new functions exist with correct signatures and behavior
- Tests cover happy path and error cases for delete/clear
- Tests cover date formatting edge cases (today, yesterday, older)
- Tests cover club summary truncation (<=3 clubs, >3 clubs)
- Build produces no TypeScript errors
- Existing EXPORT_CSV_REQUEST handler is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/15-session-history-ui/15-01-SUMMARY.md`
</output>
